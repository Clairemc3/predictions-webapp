# Setup Instructions

## Environment Variables

Set the following environment variable in your `.env` file:

### REGISTRATION_CODE
Set a string value for the `REGISTRATION_CODE` environment variable. This code will be required for user registration.

Example:
```
REGISTRATION_CODE=your_secret_code_here
```

This registration code helps control who can register for the application by requiring users to provide the correct code during registration.
